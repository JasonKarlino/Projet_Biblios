
{% extends 'base.html.twig' %}

{% block title %}Liste d'éditeurs{% endblock %}

{% block body %}
    <section class="container px-4">
        <h1>Liste d'éditeurs</h1>

        <ul class="list-group list-group-flush">
            {% for editeur in editeurs %}
                <div class="card mb-1">
                    <div class="card-body">
                        <div class="card-title d-flex justify-content-between">
                            <a href="{{ path('app_admin_editeur_show', {id: editeur.id}) }}" class="stretched-link link-dark">
                                <h4 class="mb-1">{{ editeur.nomPrenom }}</h4>
                            </a>
                            <small class="text-muted">Identifiant : {{ editeur.id }}</small>
                        </div>
                    </div>
                </div>
            {% else %}
                <li>Aucun enregistrement en base de données.</li>
            {% endfor %}
        </ul>
        {{ pagerfanta(editeurs) }}
    </section>
{% endblock %}
